# Production: full API origin (no trailing slash). Dev: leave empty to use Vite proxy.
# VITE_API_URL=https://your-api.onrender.com

# Dev only — must match backend PORT (see server/.env). Default in vite.config is :4000.
# VITE_DEV_API_PROXY=http://localhost:5000
